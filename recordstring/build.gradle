apply plugin: 'groovy'
apply plugin: 'maven'


group = 'better.recordstring'  // 组名
version = '1.0.0' // 版本

dependencies {
    compile gradleApi()
    compile localGroovy()

    compile files('lib/jxl.jar')
}

repositories {
    mavenCentral()
}

uploadArchives{
    repositories{
        mavenDeployer{
            repository(url:uri('../repo'))
            pom.groupId = 'better.recordstring' // 组名
            pom.artifactId = 'record-string' // 插件名
            pom.version = version // 版本号
        }
    }
}